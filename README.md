# PoSh-CountActiveUsersInADGroups
PowerShell function to search Active Directory for AD groups (supports wildcards) and return the number of ACTIVE user accounts in each of those AD groups, storing the results in a text file.
